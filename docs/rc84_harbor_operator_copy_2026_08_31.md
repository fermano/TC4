# Harbor RC84 operator copy

The operator panel currently says the cold-chain shipment was voided. That line is fed by support notes, not by the release artifact row.

The release artifact still needs direct code/readout reconciliation for camel-case `voidReason`. Do not use this copy-only note as evidence that the route action is fixed.
