# RC84 void gate handoff

The release branch writes route-scoped shipment actions. One previous branch handles a camel-case void reason but emits the old destination-shaped rows; another branch only changes the operator runbook text.

The active decision is whether the release artifact still drops the partner void signal, not whether the dashboard copy is tidy.
